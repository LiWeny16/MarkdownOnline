import React, { useEffect } from "react"
import Button from "@mui/material/Button"

const CodeButtonAdder = () => {
  useEffect(() => {
    // addButtonsToCodeBlocks()
  }, [])

 

  return <></>
}

export default CodeButtonAdder
